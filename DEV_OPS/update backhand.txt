
update backhand

Prompt to Claude:
Run the following commands exactly in order, one after the other.
Stop if any command fails and show me the failing command + error output.

0) delete dist
// delete the content of /vl_camtek/dist/on-prem/internal-dev/vl

rm -rf /vl_camtek/dist/on-prem/internal-dev/vl/* /vl_camtek/dist/on-prem/internal-dev/vl/.*


1) Create backend package
vl-admin dev pack --be --cpu --skip-gpu --fe

2) Uninstall
helm uninstall visual-layer
helm uninstall keycloak -n keycloak
helm uninstall openfga -n openfga

3) Delete PVCs and volumes
# first get all PVCs
kubectl get pvc -A
# then delete all PVCs in all namespaces
kubectl delete pvc --all -A

# then get all volumes (PVs)
kubectl get pv
# then delete all volumes (PVs)
kubectl delete pv --all



# delete all remains pods
kubectl delete pods -all -A

4) Install
helm upgrade -i visual-layer devops/visual-layer \
  -f devops/env/k3s/values.yaml \
  -f devops/clients/camtek/values.yaml \
  --namespace default

vl-admin install keycloak
vl-admin install openfga

5) Verify pods are running
kubectl get pods
kubectl get pods -n keycloak
kubectl get pods -n openfga

6) update launch
update launch.json , use the developer guid

