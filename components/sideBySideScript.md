Task:  
Create a UI component that matches the style of my existing app.
The component layout should follow the structure shown in the C:\Users\amita\Desktop\detaction AI\scripts\components\sideBySide.png image.
add the component to fe/clustplorer/src/views/components

Component Requirements:

The layout is split into two main sections:

Left side: displays a single large image.

Right side: displays a vertical list of images (a gallery/thumbnail column).

include left/right arrow buttons that allow the user to navigate through the list of images.

Set the navigation arrows  so they float on top of the right-side image.
Make the arrows semi-transparent, and ensure they only become visible when the mouse hovers over the image.


The right-side image list should scroll or paginate when the arrows are used.

The selected image in the right column should update the large image on the left.

The entire component must match the existing app’s visual style, including:

Colors

Typography

Spacing

Border radius

Shadow/elevation

Interaction patterns

The output should include:

Component structure

Styling (CSS/Tailwind/Styled Components—match my app’s style system)

Any necessary state logic

A short explanation of how to integrate it into the app

Important:  
Do not redesign the UI. Keep the same look & feel as the app. Only recreate the component in code.