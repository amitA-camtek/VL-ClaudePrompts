# Model training configuration
modelTraining:
  apiEndpoint: "10.20.30.40:5000"
features:
  disableAuth: true
  userManagementEnabled: "true"
  singleOrgWorkspaceEnabled: "true"
  datasetSnapshotsEnabled: "true"
  captionsFilterEnabled: "false"
oidc:
  enabled: false
dataServer:
  grpcEndpoint: "camtek-data-server:5050"
  host: "10.5.0.96"          # real IP — used for pod hostAliases
  hostname: "camtek-data-server"  # must match the hostname in grpcEndpoint
